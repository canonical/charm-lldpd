---
requires:
  juju-info:
    interface: juju-info
    scope: container
  master:
    interface: lldp

provides:
  nrpe-external-master:
    interface: nrpe-external-master
    scope: container

subordinate: true
